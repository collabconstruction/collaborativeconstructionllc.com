src/
node_modules/
.git/
.git
.claude/
.wrangler/
package.json
package-lock.json
wrangler.jsonc
.assetsignore
.gitignore
.dev.vars
.dev.vars.example
.DS_Store
**/.DS_Store

# Database schema. Not secret, but there is no reason to serve it.
schema.sql

# In a git worktree ".git" is a FILE, not a directory, so the ".git/" rule
# above does not match it and it was being served publicly.
*.sql
*.py
README.md
