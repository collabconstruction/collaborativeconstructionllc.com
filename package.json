{
  "name": "collaborativeconstructionllc-com",
  "version": "1.0.0",
  "description": "Collaborative Construction LLC website",
  "main": "worker/index.js",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@cloudflare/workers-types": "^4.20241227.0",
    "wrangler": "^4.104.0"
  }
}
